<style>
  th{
    text-align: center;
  }
</style>

<table>
  <tr>
    <th colspan="5">Movies</th>
  </tr>
  <tr>
    <th>id</th>
    <th>name</th>
    <th>sinopsis</th>
    <th>duration</th>
    <th>image</th>
  </tr>
  <% @movies.each do |movie|  %>
    <tr>
      <!-- name
      sinopsis
      duration
      image -->
      <td><%= movie.id %></td>
      <td><%= movie.name %></td>
      <td><%= movie.sinopsis %></td>
      <td><%= movie.duration %></td>
      <td><%= movie.image %></td>
    </tr>
  </table>

<% end %>

<%= render 'form' %>