<%= form_for @show, url: @path do |f| %> 
  <div class="field">
    <%= f.label :movie_id %><br>
    <%= f.collection_select(:movie_id, Movie.all, :id, :name, prompt: "Seleccione Película") %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.time_select :time %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', employee_shows_path %>